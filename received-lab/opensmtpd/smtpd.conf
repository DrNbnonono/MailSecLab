# Minimal permissive relay config for the lab:
# accept everything, relay to Mailpit's SMTP port.
listen on 0.0.0.0 port 25 hostname opensmtpd.lab.test

action "to_mailpit" relay host smtp://mailpit:1025

match from any for any action "to_mailpit"
